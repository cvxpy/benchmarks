[tool.ruff]
line-length = 100
exclude = [
    "build",
    "doc",
    "*__init__.py"
]
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.ruff.lint.isort]
known-third-party = ["cvxpy"]

[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"